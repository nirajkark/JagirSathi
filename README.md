# JagirSathi
It uses the Neural Semantic Matching Protocol. This protocol uses neural networks to interpret the semantics of job listings and candidate profile, emphasizing contextual understanding over keyword matching
